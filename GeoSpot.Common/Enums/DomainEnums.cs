namespace GeoSpot.Common.Enums;

public enum AccountType
{
    User,
    Business
};

public enum Gender
{
    NotSpecified,
    Male,
    Female,
    Other
};

public enum SpotType
{
    Promo,
    Event,
    News,
    Meetup
}

public enum Platform
{
    Ios,
    Android
}